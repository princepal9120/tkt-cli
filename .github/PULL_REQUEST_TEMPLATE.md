## What

Brief description of what this PR changes.

## Why

The motivation — bug fix, missing feature, reliability improvement.

## Testing

How you verified it works:
- [ ] `bunx tsc --noEmit` passes
- [ ] `bun test` passes
- [ ] Tested manually with `bun run dev -- <command>`

## Checklist

- [ ] README updated (if adding/changing a command)
- [ ] No binary committed (`tkt` file)
- [ ] One logical change per PR
